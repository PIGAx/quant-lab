name: Daily Signal Scan

on:
  schedule:
    - cron: "50 5 * * 1-5"
    - cron: "30 21 * * 1-5"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install deps
        run: pip install yfinance pandas numpy requests

      - name: Run scanner
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python signals/scanner.py

      - name: Commit signal state
        run: |
          git config user.name "qlab-bot"
          git config user.email "qlab-bot@users.noreply.github.com"
          git add data/signal_state.json
          git diff --cached --quiet || git commit -m "chore: update signal state [skip ci]"
          git push
